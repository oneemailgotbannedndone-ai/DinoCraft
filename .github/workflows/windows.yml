name: Windows

# Builds DinoCraft for Windows and Mac. On pushes to main (or when "publish" is ticked) both
# downloads are published on this repository's Releases page, where the launcher's update button finds them.

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      publish:
        description: Publish this build on the Releases page
        type: boolean
        default: false

jobs:
  build:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Swift
        uses: compnerd/gha-setup-swift@main
        with:
          swift-version: swift-6.1-release
          swift-build: 6.1-RELEASE

      - name: Swift version
        run: swift --version

      - name: Stamp build number
        shell: pwsh
        run: |
          $commit = "${{ github.sha }}".Substring(0, 7)
          $date = Get-Date -Format yyyy-MM-dd
          "{`"build`": ${{ github.run_number }}, `"commit`": `"$commit`", `"date`": `"$date`"}" | Out-File Resources/Data/build.json -Encoding ascii
          Get-Content Resources/Data/build.json

      - name: Build core self-test
        run: swift build -c release --product DinoCraftSelfTest

      - name: Run core self-test
        run: .build\release\DinoCraftSelfTest.exe

      - name: Download SDL3
        shell: pwsh
        run: |
          $tag = gh release list --repo libsdl-org/SDL --limit 40 --json tagName,isPrerelease --jq '[.[] | select(.isPrerelease | not) | .tagName | select(startswith("release-3."))][0]'
          Write-Host "SDL release: $tag"
          New-Item -ItemType Directory -Force third_party | Out-Null
          gh release download $tag --repo libsdl-org/SDL --pattern "SDL3-devel-*-VC.zip" --dir third_party
          $zip = Get-ChildItem third_party -Filter "SDL3-devel-*-VC.zip" | Select-Object -First 1
          Expand-Archive $zip.FullName -DestinationPath third_party -Force
          $sdl = (Get-ChildItem third_party -Directory -Filter "SDL3-*" | Select-Object -First 1).FullName -replace '\\', '/'
          Write-Host "SDL folder: $sdl"
          "SDL3_DIR=$sdl" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build DinoCraft for Windows
        shell: pwsh
        # An 8 MB main-thread stack (Windows' default is 1 MB).
        run: swift build -c release --product DinoCraftWin -Xcc "-I$env:SDL3_DIR/include" -Xlinker "-L$env:SDL3_DIR/lib/x64" -Xlinker "/STACK:8388608"

      - name: Package
        shell: pwsh
        run: |
          $out = "DinoCraft-Windows"
          New-Item -ItemType Directory -Force "$out/Resources/Textures" | Out-Null
          Copy-Item .build/release/DinoCraftWin.exe "$out/DinoCraft.exe"
          Copy-Item .build/release/DinoCraftWin.exe "$out/DinoCraft Launcher.exe"
          Copy-Item "$env:SDL3_DIR/lib/x64/SDL3.dll" $out
          $roots = @("$env:LOCALAPPDATA\Programs\Swift", "C:\Program Files\Swift", "C:\Library") | Where-Object { Test-Path $_ }
          $runtime = Get-ChildItem -Path $roots -Recurse -Filter swiftCore.dll -ErrorAction SilentlyContinue |
            Sort-Object { if ($_.FullName -match 'Runtimes') { 0 } else { 1 } } | Select-Object -First 1
          if (-not $runtime) { throw "Swift runtime not found" }
          Write-Host "Swift runtime: $($runtime.DirectoryName)"
          Copy-Item "$($runtime.DirectoryName)\*.dll" $out
          foreach ($dll in @("vcruntime140.dll", "vcruntime140_1.dll", "msvcp140.dll")) {
            if (Test-Path "C:\Windows\System32\$dll") { Copy-Item "C:\Windows\System32\$dll" $out }
          }
          Copy-Item Resources/Data "$out/Resources/Data" -Recurse
          Copy-Item Resources/Textures/blocks "$out/Resources/Textures/blocks" -Recurse
          Copy-Item Resources/Textures/items "$out/Resources/Textures/items" -Recurse
          Copy-Item Resources/TexturePacks "$out/Resources/TexturePacks" -Recurse
          Copy-Item Resources/Sounds "$out/Resources/Sounds" -Recurse
          Copy-Item Resources/Music "$out/Resources/Music" -Recurse
          Remove-Item "$out/Resources/Music/*.m4a" -ErrorAction SilentlyContinue
          Get-ChildItem $out | Format-Table Name, Length

      - name: Automated check with software OpenGL
        shell: pwsh
        timeout-minutes: 15
        run: |
          gh release download --repo pal1000/mesa-dist-win --pattern "mesa3d-*-release-msvc.7z" --dir mesa
          $archive = Get-ChildItem mesa -Filter *.7z | Select-Object -First 1
          7z x $archive.FullName -omesa/x -y | Out-Null
          Copy-Item DinoCraft-Windows smoke -Recurse
          Copy-Item mesa/x/x64/*.dll smoke -Force
          $env:GALLIUM_DRIVER = "llvmpipe"
          $env:DINOCRAFT_HOME = "$PWD/smoke-home"
          $shot = "$PWD/screenshot.png"
          $p = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shot`" --frames 20 --render-distance 5 --seed dino --demo-entities" -PassThru -Wait -NoNewWindow
          Write-Host "Exit code: $($p.ExitCode)"
          Get-ChildItem smoke-home/logs -ErrorAction SilentlyContinue | ForEach-Object { Get-Content $_.FullName -Tail 40 }
          if (-not (Test-Path $shot)) { throw "The game did not produce a screenshot" }
          $shot2 = "$PWD/screenshot-inventory.png"
          $p2 = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shot2`" --frames 5 --render-distance 3 --seed dino --demo-entities --demo-screen inventory" -PassThru -Wait -NoNewWindow
          Write-Host "Inventory check exit code: $($p2.ExitCode)"
          if (-not (Test-Path $shot2)) { throw "The game did not produce the inventory screenshot" }
          $shot3 = "$PWD/screenshot-menu.png"
          $p3 = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shot3`" --demo-screen menu" -PassThru -Wait -NoNewWindow
          Write-Host "Menu check exit code: $($p3.ExitCode)"
          if (-not (Test-Path $shot3)) { throw "The game did not produce the menu screenshot" }
          $shot4 = "$PWD/screenshot-worlds.png"
          $p4 = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shot4`" --demo-screen worlds --hide-console" -PassThru -Wait -NoNewWindow
          Write-Host "World list check exit code: $($p4.ExitCode)"
          if (-not (Test-Path $shot4)) { throw "The game did not produce the world list screenshot" }
          foreach ($screen in @("creative", "furnace", "pause", "settings", "controls", "crafting", "double-chest", "villagers", "dinos", "pets", "boat", "mining", "chat", "nursery", "armory", "sky", "enchanting", "quests", "questbook", "xp", "digsite", "museum", "meteors", "storm", "map", "circuits", "decorations", "autumn", "winter", "fire", "shipwreck", "treasure", "newdinos", "explorer")) {
            $shotN = "$PWD/screenshot-$screen.png"
            $pN = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shotN`" --frames 5 --render-distance 4 --seed dino --demo-entities --demo-screen $screen" -PassThru -Wait -NoNewWindow
            Write-Host "$screen check exit code: $($pN.ExitCode)"
            if (-not (Test-Path $shotN)) { throw "The game did not produce the $screen screenshot" }
          }
          # Under the sea: a coral reef with fish (the camera dives there, then the chunks load).
          $shotR = "$PWD/screenshot-reef.png"
          $pR = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shotR`" --frames 120 --render-distance 4 --seed dino --demo-screen reef" -PassThru -Wait -NoNewWindow
          Write-Host "Reef check exit code: $($pR.ExitCode)"
          if (-not (Test-Path $shotR)) { throw "The game did not produce the reef screenshot" }
          # Multiplayer: host a world, join it from a second copy of the game (the full shared game on both ends).
          $hostP = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--host Alice --render-distance 4 --seed dino --hide-console" -PassThru -NoNewWindow
          Start-Sleep -Seconds 30
          $shotJ = "$PWD/screenshot-join.png"
          $pJ = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--join 127.0.0.1 --name Bob --screenshot `"$shotJ`" --frames 60 --render-distance 4 --hide-console" -PassThru -Wait -NoNewWindow
          Write-Host "Join check exit code: $($pJ.ExitCode)"
          Stop-Process -Id $hostP.Id -Force -ErrorAction SilentlyContinue
          if (-not (Test-Path $shotJ)) { Write-Host "::warning::Joining a hosted world did not produce a screenshot" }
          # The launcher's online work (updates, reviews, friends, sharing stats) and the leaderboard, for real.
          # The leaderboard address is changed so nothing is posted to the real board.
          $env:DINOCRAFT_LEADERBOARD_URL = "http://dreamlo.com/lb/automated-check"
          '{"playSeconds":120,"deaths":1,"kills":2,"bossesBeaten":0,"blocksMined":5,"blocksPlaced":3,"itemsCrafted":1,"metresWalked":40,"foodEaten":0}' | Out-File -FilePath smoke-home/stats.json -Encoding ascii
          foreach ($screen in @("menu", "leaderboard")) {
            $shotO = "$PWD/screenshot-online-$screen.png"
            $pO = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shotO`" --demo-screen $screen --online --frames 400" -PassThru -Wait -NoNewWindow
            Write-Host "Online $screen check exit code: $($pO.ExitCode)"
            if (-not (Test-Path $shotO)) { throw "The game did not produce the online $screen screenshot" }
          }
          Remove-Item Env:DINOCRAFT_LEADERBOARD_URL
          $shotC = "$PWD/screenshot-create.png"
          $pC = Start-Process -FilePath "smoke/DinoCraft.exe" -ArgumentList "--screenshot `"$shotC`" --demo-screen create --hide-console" -PassThru -Wait -NoNewWindow
          Write-Host "Create world check exit code: $($pC.ExitCode)"
          if (-not (Test-Path $shotC)) { throw "The game did not produce the create world screenshot" }
          Get-ChildItem smoke-home/logs -ErrorAction SilentlyContinue | ForEach-Object { Select-String -Path $_.FullName -Pattern "Audio|Music|error" | Select-Object -Last 20 }

      - name: Upload screenshot and logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: automated-check
          path: |
            screenshot.png
            screenshot-inventory.png
            screenshot-menu.png
            screenshot-worlds.png
            screenshot-creative.png
            screenshot-furnace.png
            screenshot-pause.png
            screenshot-settings.png
            screenshot-create.png
            screenshot-crafting.png
            screenshot-double-chest.png
            screenshot-reef.png
            screenshot-controls.png
            screenshot-villagers.png
            screenshot-dinos.png
            screenshot-pets.png
            screenshot-boat.png
            screenshot-mining.png
            screenshot-chat.png
            screenshot-nursery.png
            screenshot-armory.png
            screenshot-sky.png
            screenshot-enchanting.png
            screenshot-quests.png
            screenshot-questbook.png
            screenshot-xp.png
            screenshot-digsite.png
            screenshot-museum.png
            screenshot-meteors.png
            screenshot-storm.png
            screenshot-map.png
            screenshot-circuits.png
            screenshot-decorations.png
            screenshot-autumn.png
            screenshot-winter.png
            screenshot-fire.png
            screenshot-shipwreck.png
            screenshot-treasure.png
            screenshot-newdinos.png
            screenshot-explorer.png
            screenshot-join.png
            screenshot-online-menu.png
            screenshot-online-leaderboard.png
            .build/release/*.pdb
            smoke-home/logs/
          if-no-files-found: ignore

      - name: Upload Windows game
        uses: actions/upload-artifact@v4
        with:
          name: DinoCraft-Windows
          path: DinoCraft-Windows

      - name: Zip for the launcher's updater
        shell: pwsh
        run: Compress-Archive -Path DinoCraft-Windows/* -DestinationPath DinoCraft-Windows.zip

      - name: Upload Windows zip
        uses: actions/upload-artifact@v4
        with:
          name: windows-zip
          path: DinoCraft-Windows.zip

  mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Stamp build number
        run: |
          echo "{\"build\": ${{ github.run_number }}, \"commit\": \"${GITHUB_SHA::7}\", \"date\": \"$(date +%F)\"}" > Resources/Data/build.json
          cat Resources/Data/build.json

      - name: Build DinoCraft.app
        run: zsh Scripts/build_app.sh

      - name: Compile the Metal shaders
        # The game compiles its shaders at startup, so check them here where a mistake can't reach players.
        run: |
          cat Resources/Shaders/*.metal > "$RUNNER_TEMP/all.metal"
          set +e
          out=$(xcrun -sdk macosx metal -std=metal3.0 -c "$RUNNER_TEMP/all.metal" -o "$RUNNER_TEMP/all.air" 2>&1)
          code=$?
          set -e
          echo "$out"
          if [ $code -ne 0 ]; then
            if echo "$out" | grep -qi "toolchain\|unable to find utility\|not a developer tool"; then
              echo "::warning::Metal compiler not available on this runner; shader check skipped"
            else
              echo "::error::Metal shaders failed to compile"
              exit 1
            fi
          fi

      - name: Zip for the launcher's updater
        run: |
          mkdir -p mac-zip
          ditto dist/DinoCraft.app mac-zip/DinoCraft.app
          ditto "dist/DinoCraft Launcher.app" "mac-zip/DinoCraft Launcher.app"
          ditto -c -k mac-zip DinoCraft-Mac.zip

      - name: Upload Mac zip
        uses: actions/upload-artifact@v4
        with:
          name: mac-zip
          path: DinoCraft-Mac.zip

  release:
    needs: [build, mac]
    if: always() && needs.build.result == 'success' && (github.event_name == 'push' || inputs.publish)
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download Windows zip
        uses: actions/download-artifact@v4
        with:
          name: windows-zip
          path: out

      - name: Download Mac zip
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: mac-zip
          path: out

      - name: Publish the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo "## Download"
            echo ""
            echo "- **Windows:** download **DinoCraft-Windows.zip** below, right-click it and choose *Extract All*, then open **DinoCraft Launcher.exe** (if Windows warns you, click *More info* → *Run anyway*)."
            echo "- **Mac (Apple Silicon):** download **DinoCraft-Mac.zip** below, double-click it, drag **DinoCraft.app** and **DinoCraft Launcher.app** to Applications and open the launcher (the first time: System Settings → Privacy & Security → *Open Anyway*)."
            echo ""
            echo "Already playing? The launcher's **Update** button installs this version for you."
            echo ""
            echo "## What's new"
            echo ""
            if [ -s Resources/Data/whatsnew.txt ]; then cat Resources/Data/whatsnew.txt; else git log -1 --pretty=%B; fi
          } > notes.txt
          ls -la out
          gh release create "build-${{ github.run_number }}" out/*.zip \
            --repo "${{ github.repository }}" \
            --target "${{ github.sha }}" \
            --title "DinoCraft build ${{ github.run_number }}" \
            --notes-file notes.txt
